# Pico2KVM firmware

RP2350 firmware (Pico SDK / TinyUSB / lwIP+altcp / mbedTLS). The device
connects outbound over WSS to the Cloudflare Worker relay and exposes a USB
HID keyboard to the host it is plugged into. See the repository-root README
for the protocol and full setup.
